# Selecting Elements Exercise 1

## Instructions

For the JavaScript to manipulate the DOM, we shall start with JavaScript code inside the HTML document
The JavaScript Code will be inside `<script> </script>` tags

Edit the code within the script tags

Try:

- Selecting all the elements with class = "print" and assign it to variable print
  - The code to print these elements has already been done

- Now select the elements with id = "special" and assign it to variable print

## Running the Test

To test if your code is correct, open the HTML file in Chrome.
Right click on the page and click inspect.
Click on Console to see what is bring printed by console.log
Now refresh the page!

The result for each test case will be printed in the console.
