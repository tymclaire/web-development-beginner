# Event Delegation Exercise 1

## Instructions

For the JavaScript to manipulate the DOM, we shall start with JavaScript code inside the HTML document
The JavaScript Code will be inside `<script> </script>` tags

Edit the code within the script tags

Try:

- Task 1: You have the 4 different buttons. Implement a click event listener with event delegation to have unique behaviour for each button.
  - Button 1 should have the alert "Button 1 was clicked"
  - Button 2 should have the alert "Button 1 was pressed"
  - Button 3 should have the alert "You pressed Button 3"
  - Button 4 should have the alert "Why did you press Button 4?"

## Running the Test

To test if your code is correct, open the HTML file in Chrome.
Now refresh the page!

Make sure the correct elements respond when clicked, and moused over.
